import 'package:flutter/material.dart';

abstract class ThemeColor {
  static const primaryColor = Colors.brown;
  static const foregroundColor = Colors.white;

  static Color? tileColor = Colors.grey[200];
}
